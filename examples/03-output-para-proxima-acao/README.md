# 03 - Output para próxima ação

Este exemplo usa duas ações pequenas.

1. `step-1-normalizar-dados.js` limpa e normaliza inputs.
2. `step-2-classificar.js` recebe outputs do passo anterior e classifica o registro.

O objetivo é mostrar o padrão de encadeamento: output de um código vira input do próximo.

